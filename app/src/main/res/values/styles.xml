<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
      Compose 应用：宿主主题只需提供无 ActionBar 的窗口，
      实际配色（含动态取色、深色模式）由 Miuix 在 Compose 层处理。
    -->
    <style name="Theme.KuaiSnapPlus" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">#FFF7F7F7</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

</resources>
