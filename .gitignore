# 适配用的参考 APK（体积大，不提交）
/7.7.0.apk
/6.77.0.apk

# 逆向分析与构建产物（本地使用，不提交）
/re/
/.tools/

# 签名密钥与产物（请勿提交）
/keystore/
/dist/
*.idsig

# Gradle / Android 构建产物
/.gradle/
/.kotlin/
/build/
/app/build/
/local.properties

# 旧手动构建流程的临时目录（已废弃，防止陈旧产物被误提交）
/build_tmp/
